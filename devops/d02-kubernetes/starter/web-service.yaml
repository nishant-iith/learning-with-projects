# TODO: Define a Service (NodePort or LoadBalancer) to route external traffic to your Web Pods
# apiVersion: v1
# kind: Service
# metadata:
#   name: web-service
# spec:
#   type: NodePort
#   ports:
#     - port: 80
#       targetPort: 3000
#       nodePort: 30080
#   selector:
#     app: web
