export class Calculator {
  /**
   * Adds comma-separated numbers in an expression string.
   * Empty string returns 0.
   * E.g. "1,2" returns 3.
   */
  add(expression: string): number {
    // TODO: Implement the TDD step.
    // 1. If expression is empty, return 0.
    // 2. Split string by comma delimiter.
    // 3. Map string array to numbers, sum them up and return.
    return 0;
  }
}
